# Optional MapLibre style URL. Leave empty for a blank dark canvas (no tiles).
VITE_BASEMAP_STYLE=

# Vite base path for GitHub Pages project sites (e.g. /mesh-fire-detection).
# Locally leave empty or /.
BASE_PATH=
